{% extends 'base.html.twig' %}

{% block title %}Ajouter un match{% endblock %}

{% block body %}
    {{ include('app/home/navbar.html.twig') }}
    <div class="container">
        <div class="row">
            {{ form_start(form) }}
            {{ form_widget(form) }}
            <div class="form-group">
                <input type="submit" value="Ajouter" class="btn btn-primary float-right pt-4"/>
            </div>
            {{ form_end(form) }}
        </div>
    </div>
    {{ include('app/home/footer.html.twig') }}
{% endblock %}
