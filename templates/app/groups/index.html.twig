{% extends 'base.html.twig' %}

{% block title 'FriendsBet - Votre groupe' %}

{% block body %}
    {{ include('app/home/navbar.html.twig') }}
    <div class="container mt-5">
        <div class="row">
            <h1 class="text-center group_name">{{ group.name ?? null }}</h1>
        </div>
        <div class="row d-flex align-content-center align-items-center mt-4">
            <div class="col-3">
                <ul class="nav flex-column lighten-3 py-4 mb-r z-depth-1">
                    <li class="nav-item nav-group-left">
                        <a class="nav-link active" href="{{ path('groups.index') }}"><i class="fas fa-user-friends"></i> Mon groupe</a>
                    </li>
                    <li class="nav-item nav-group-left">
                        <a class="nav-link" href="{{ path('groups.stats') }}"><i class="fas fa-chart-line"></i> Classement</a>
                    </li>
                    <li class="nav-item nav-group-left">
                        <a class="nav-link" href="{{ path('groups.index') }}"><i class="fas fa-clock"></i> Historique</a>
                    </li>
                    <li class="nav-item nav-group-left">
                        <a class="nav-link" href="{{ path('groups.settings') }}"><i class="fas fa-cogs"></i> Paramètres</a>
                    </li>
                </ul>
            </div>
            <div class="col-6 text-center">
                {% if form is defined and form is not null %}
                    {{ form_start(form) }}
                    {{ form_widget(form) }}
                    <div class="form-group py-4">
                        <input type="submit" value="Créer ton groupe" class="btn btn-success float-right">
                    </div>
                    {{ form_end(form) }}
                {% else %}
                    {% if group.token is defined and group.token is not null %}
                        <h4>Envoyez ce lien à vos amis pour qu'ils rejoignent votre groupe : <span class="token">http://jordan.renard/groups/join/{{ group.token }}</span></h4>
                    {% endif %}
                    <button type="button" class="btn btn-access login-btn-center mt-5" data-toggle="modal" data-target="#staticBackdrop">
                        Quittez ce groupe en cliquant ici
                    </button>
                {% endif %}
            </div>
        </div>
    </div>
{% endblock %}
